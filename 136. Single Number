class Solution {
    public int singleNumber(int[] nums) {
       // non-empty array of integers nums
       // appears twice 

       int single = 0;
       for(int num : nums){
        single ^= num;
       }
       return single;
    }
}
